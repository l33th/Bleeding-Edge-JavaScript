let person = {
	section: 'Web',
	title: 'Senior Developer',
	exp: 3
};

const {section, title, exp} = person;

console.log(`Section: ${section} \nTitle: ${title} \nYears of Experience: ${exp}`);
console.log(`${section} ${title} of ${exp} years`);
